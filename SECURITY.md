# Security

Do not report credentials through a public issue. Contact the repository owner privately if a credential or private key is accidentally published.

This starter intentionally excludes secrets and uses local public fixtures. Forks should use an encrypted secret manager and should scan changes before publishing them.

Before opening a pull request, confirm that it does not contain private keys, passwords, tokens, production certificates, private host history, or proprietary session data.
