# Certificate fixture

The public starter uses a disposable demonstration CA certificate so the flake can evaluate without private infrastructure. Replace it with your own certificate source or remove `security.pki.certificateFiles` when customizing the configuration.
