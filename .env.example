# Optional host selected by the container lab.
RENIX_HOST=demo

# Optional fork cloned into a new Docker-managed workspace.
# RENIX_REPO=https://github.com/dmasiero/renix-public-starter.git

# Optional explicit platform for a new host.
# RENIX_DEMO_SYSTEM=x86_64-linux
