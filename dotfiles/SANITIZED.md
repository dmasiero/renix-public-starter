# Sanitized dotfiles snapshot

This public starter contains a representative, secret-free subset of the dotfiles used by Renix.

Credentials, SSH material, host history, communication-app state, private service configuration, and AI session data were intentionally omitted. The complete private recovery set is stored separately and is not part of this repository.
